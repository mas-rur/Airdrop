# Same testnet RPC as testnet-dashboard -- this app only ever reads public
# on-chain data (balance, transaction history), it never needs a faucet key.
NEXT_PUBLIC_RPC_URL=https://pay3-testnet-production.up.railway.app

# From your Supabase project: Settings -> API. The anon key is a public,
# rate-limited client key (safe to expose to the browser) -- it is NOT the
# service_role key, which must never end up in NEXT_PUBLIC_* variables.
# Run supabase/schema.sql once in the Supabase SQL editor before using this.
NEXT_PUBLIC_SUPABASE_URL=https://ddzjdmvtmhqeakphbwvg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_6k2bGIE8HjH4HvquECp-yw_xE3Tt9Th
